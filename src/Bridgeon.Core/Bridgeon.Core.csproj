<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <!-- Declared here, not only in Directory.Build.props: Stryker's project
         analysis cannot see a target framework that lives only in the props
         file. An architecture test keeps every project's declaration equal. -->
    <TargetFramework>net10.0</TargetFramework>
  </PropertyGroup>

</Project>
